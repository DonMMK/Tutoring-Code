%% Thinking process
% To know how to use the integral function type 
% doc integral in the command window

%% Solution 
function Area_integral = integrate(Func,a,b)
    %apply the integral function
    % the area under the grtaph is the integration across the limits
    Area_integral = integral(Func,a,b);
    
end